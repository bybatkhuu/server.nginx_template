version: "3.8"

services:
  nginx:
    image: voiceprint/nginx:latest
    build:
      context: .
    restart: unless-stopped
    environment:
      - TERM=${TERM:-xterm}
      - TZ=${TZ:-Asia/Seoul}
    volumes:
      - "./volumes/storage/nginx/ssl:/etc/nginx/ssl"
    network_mode: host
    tty: true
